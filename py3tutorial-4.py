game = [[0,0,0],
        [0,0,0],
        [0,0,0],]

print('   a  b  c')
for count,row in enumerate(game): #enumerate is for giving list with their index no.
    print(count,row)
    '''for item in row:
        print(item)''' #for nested loop
# we can use (''' ''') for multi line comments